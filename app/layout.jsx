export const metadata = {
  title: "Dhakad Doctors Association",
  description: "Official portal",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}

